enum InteractableType
{
    Normal = 0,
    Point = 1,
    Attack = 2,
    Mine = 3,
    Talk = 4,
    Null = 5,
}

enum ButtonState
{
    Idle,
    Hover,
    Click
}

enum MenuState
{
    Opened,
    Closed
}