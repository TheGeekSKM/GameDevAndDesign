if (array_length(buttons) != array_length(obj_questList.buttons))
{
    buttons = obj_questList.buttons;
}

if (selectedIndex != obj_questList.selectedIndex)
{
    selectedIndex = obj_questList.selectedIndex;
}