// Add some loading screen graphics here...
scribble("Loading...")
    .align(fa_center, fa_middle)
    .starting_format("VCR_OSD_Mono", c_white)
    .transform(1, 1, 0)
    .draw(GUI_DEFAULT_WIDTH / 2, GUI_DEFAULT_HEIGHT / 2);